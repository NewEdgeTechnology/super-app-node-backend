{
  "name": "new-edge",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd driver && npm install && cd ../map_and_location_services && npm install && cd ../ride_management && npm install && cd ..",
    "start:registration": "cd driver && npm run dev",
    "start:location": "cd map_and_location_services && npm run dev",
    "start:ride": "cd ride_management && npm run dev",
    "start:all": "npm run install:all && concurrently \"npm run start:registration\" \"npm run start:location\" \"npm run start:ride\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
